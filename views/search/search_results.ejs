<!-- This page returns the search results -->
<!doctype html>
<html>
  <head>
    <title>Search Results</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <nav> 
      <p1><a href="/"> Return to Home</a></p1>  <!-- Link to the home page -->
      <button id="darkmode">Enable Dark Mode</button> <!-- Button to toggle dark mode -->
    </nav>
    <h1>Search Results</h1>
    <h2>Here is a list of any matching items on our menu:</h2>
    <% if (menuItems.length === 0) { %>
        <p>No items found matching your search.</p> <!-- Message displayed if no items match -->
    <% } %>
    <ul>
      <% menuItems.forEach(function(item) { %>
        <li><%= item.name %>, £<%= item.price%> </li> <!-- Displays all matching items -->
      <% }) %>
    </ul>
    <p><a href="/menu/search">Search again</a></p> <!-- Allows user to search again -->
    <script src="/darkmode.js"></script> <!-- Link to dark mode script -->
  </body>
</html>
