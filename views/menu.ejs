<!doctype html>
<html>
  <head>
    <title>Umair's Restaurant Menu</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Umair's Restaurant Menu</h1>
    <h2>Here are all of the items on our menu:</h2>
    <ul>
      <% menuItems.forEach(function(item) { %>
        <li><%= item.name %>, £<%= item.price%> </li>
      <% }) %>
    </ul>
    </body>
</html>
